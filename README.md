
  # Portfolio page with resume

  This is a code bundle for Portfolio page with resume. The original project is available at https://www.figma.com/design/J0kR8grUpN49x37Zf9ajnN/Portfolio-page-with-resume.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  